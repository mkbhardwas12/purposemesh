{
  "name": "@cca/pep-adapter-example",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "TypeScript Policy Enforcement Point example for PurposeMesh (Technical Preview)",
  "type": "module",
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
